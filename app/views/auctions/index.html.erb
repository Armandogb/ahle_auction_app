
<div id="accordion">

  <% @sections.each do |section| %>
    <div class="card">
      <div class="card-header" id="headingOne">
        <h3 class="mb-0">
          <div class="btn btn-link" data-toggle="collapse" data-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
            <div class="row">
              <div class="col-9">
                <i class="fa fa-arrow-circle-down" aria-hidden="true"></i> <%= section.display_name %>
              </div>
              <div class="col-3">
                <%= section.end_time %>
              </div>
            </div>
          </div>
        </h3>
      </div>
      <div id="collapseOne" class="collapse collapsed" aria-labelledby="headingOne" data-parent="#accordion">
        <div class="card-body">
          <% section.items.each do |item| %>
            <%= render partial: "auctions/partials/auction_items", locals: { item: item, section: section } %>
          <% end %>
        </div>
      </div>
    </div>`
  <% end %>
</div>